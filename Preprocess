Preprocesses the VinDr-Mammo dataset by merging metadata and annotations, performing a stratified train/test split, creating PyTorch datasets and dataloaders, and visualizing images with BI-RADS labels and bounding boxes.
